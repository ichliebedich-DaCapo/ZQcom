<Window x:Class="ZQcom.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZQcom.Views" xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="ZQcom" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Serial Port View -->
        <StackPanel Grid.Column="0" Margin="10">
            <ComboBox ItemsSource="{Binding SerialPortViewModel.AvailablePorts}"
                      SelectedItem="{Binding SerialPortViewModel.SelectedPort}"
                      Margin="0,0,0,10"/>
            <Button Content="Open Serial Port"
                    Command="{Binding SerialPortViewModel.OpenCommand}"
                    Margin="0,0,0,10"/>
            <TextBox Text="{Binding SerialPortViewModel.LogText, Mode=OneWay}"
                     IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     AcceptsReturn="True"
                     Height="200"/>
        </StackPanel>

        <!-- Chart View -->
        <StackPanel Grid.Column="1" Margin="10">
            <lvc:CartesianChart Series="{Binding ChartViewModel.ChartModel.Series}" Height="379">
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis Labels="{Binding ChartViewModel.ChartModel.Labels}"/>
                </lvc:CartesianChart.AxisX>
                <lvc:CartesianChart.AxisY>
                    <lvc:Axis LabelFormatter="{Binding ChartViewModel.ChartModel.YFormatter}"/>
                </lvc:CartesianChart.AxisY>
            </lvc:CartesianChart>
            <TextBox Text="Enter point count" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>